apiVersion: 1

# Questo file dice a Grafana di cercare dashboard JSON nella cartella dove monterai i tuoi file .json.

providers:
  - name: 'Preloaded dashboards'
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
